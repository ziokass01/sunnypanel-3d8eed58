--- /mnt/data/sunnypanel_full/sunnypanel-3d8eed58-main/.github/workflows/supabase-deploy.yml	2026-06-26 09:43:14.000000000 +0000
+++ /mnt/data/sunnypanel_web_final/.github/workflows/supabase-deploy.yml	2026-07-02 15:56:41.502205725 +0000
@@ -1,9 +1,6 @@
-name: Deploy Supabase (DB + Edge Functions)
+name: Manual Supabase DB Push + Edge Functions
 
 on:
-  push:
-    branches:
-      - main
   workflow_dispatch:
     inputs:
       run_db_push:
@@ -11,6 +8,13 @@
         required: false
         default: "false"
 
+permissions:
+  contents: read
+
+concurrency:
+  group: supabase-production-deploy
+  cancel-in-progress: false
+
 jobs:
   deploy:
     runs-on: ubuntu-latest
@@ -21,25 +25,45 @@
 
     steps:
       - uses: actions/checkout@v4
-      - uses: supabase/setup-cli@v1
+
+      # Pin both the action channel and CLI version. This avoids the unauthenticated
+      # "resolve latest release" request that was hitting GitHub API rate limits.
+      - name: Set up Supabase CLI
+        uses: supabase/setup-cli@v2
         with:
-          version: latest
+          version: 2.84.2
+          github-token: ${{ github.token }}
+
+      - name: Verify Supabase CLI
+        run: supabase --version
+
+      - name: Validate required deployment secrets
+        shell: bash
+        run: |
+          set -euo pipefail
+          test -n "$SUPABASE_ACCESS_TOKEN" || { echo "::error::Missing SUPABASE_ACCESS_TOKEN"; exit 1; }
+          test -n "$SUPABASE_DB_PASSWORD" || { echo "::error::Missing SUPABASE_DB_PASSWORD"; exit 1; }
+          test -n "$PROJECT_REF" || { echo "::error::Missing SUPABASE_PROJECT_REF"; exit 1; }
+
       - name: Link project
         run: supabase link --project-ref "$PROJECT_REF" --password "$SUPABASE_DB_PASSWORD"
+
       - name: Show migration list
         run: supabase migration list
+
       - name: Repair legacy migration markers 20260327 and 20260327120000
-        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.run_db_push == 'true' }}
+        if: ${{ github.event.inputs.run_db_push == 'true' }}
         run: |
           supabase migration repair --status reverted 20260327 || true
           supabase migration repair --status reverted 20260327120000 || true
+
       - name: Push migrations
         id: db_push
-        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.run_db_push == 'true' }}
+        if: ${{ github.event.inputs.run_db_push == 'true' }}
         continue-on-error: true
         run: yes | supabase db push --include-all
+
       - name: Deploy public/admin Edge Functions with verify_jwt disabled
-        if: ${{ always() && !cancelled() }}
         run: |
           set -euo pipefail
           no_verify_functions=(
@@ -54,8 +78,8 @@
               supabase functions deploy "$fn" --project-ref "$PROJECT_REF" --no-verify-jwt
             fi
           done
+
       - name: Deploy remaining Edge Functions with default verify_jwt
-        if: ${{ always() && !cancelled() }}
         run: |
           set -euo pipefail
           remaining_functions=(mobile-auth-email)
@@ -65,18 +89,19 @@
               supabase functions deploy "$fn" --project-ref "$PROJECT_REF"
             fi
           done
+
       - name: Set function secrets
-        if: ${{ always() && !cancelled() }}
         run: |
-          supabase secrets set \
+          supabase secrets set --project-ref "$PROJECT_REF" \
             ADMIN_EMAILS="${{ secrets.ADMIN_EMAILS }}" \
             PUBLIC_BASE_URL="${{ secrets.PUBLIC_BASE_URL }}" \
             VERIFY_HMAC_SECRET="${{ secrets.VERIFY_HMAC_SECRET }}" \
             TURNSTILE_SITE_KEY="${{ secrets.TURNSTILE_SITE_KEY }}" \
             TURNSTILE_SECRET_KEY="${{ secrets.TURNSTILE_SECRET_KEY }}" \
             RUNTIME_OPS_ADMIN_KEY="${{ secrets.RUNTIME_OPS_ADMIN_KEY }}"
+
       - name: Warn if db push failed
-        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.run_db_push == 'true' && steps.db_push.outcome == 'failure' }}
+        if: ${{ github.event.inputs.run_db_push == 'true' && steps.db_push.outcome == 'failure' }}
         run: |
-          echo "::warning::supabase db push failed. Functions were deployed. If Free Key provider table is missing, run HOTFIX_FREE_KEY_RUN_IN_SQL_EDITOR.sql in Supabase SQL Editor."
+          echo "::warning::supabase db push failed. Functions were deployed. If the Free Key provider table is missing, run HOTFIX_FREE_KEY_RUN_IN_SQL_EDITOR.sql in Supabase SQL Editor."
           exit 0
